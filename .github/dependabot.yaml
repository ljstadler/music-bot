version: 2
updates:
    - package-ecosystem: "github-actions"
      directory: "/"
      schedule:
          interval: "weekly"
      groups:
          actions-dependencies:
              patterns:
                  - "*"

    - package-ecosystem: "uv"
      directory: "/"
      schedule:
          interval: "weekly"
      groups:
          dependencies:
              patterns:
                  - "*"
